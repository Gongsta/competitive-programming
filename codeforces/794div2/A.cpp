#include <bits/stdc++.h>

using namespace std;

// I actually had no idea for this one...
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

	int t;
	cin >> t;
	int n;
	while (t--) {
		cin >> n;

	}
    return 0;
}

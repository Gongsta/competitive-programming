#include <bits/stdc++.h>

using namespace std;

int main() {
    int N, C, H;

    cin >> N >> C;

    while (N--) {
        cin >> H;
        cout << H + 2  * C << " ";
    }
    return 0;
}

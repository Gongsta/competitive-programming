#include <bits/stdc++.h>

typedef long long ll;

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int N,C,Q;
    cin >> N >> C >> Q;
    int l = 0;
    int r = C;
    cout << "?" << l << " " << r << endl;


    return 0;
}

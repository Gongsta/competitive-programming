#include <bits/stdc++.h>

typedef long long ll;

using namespace std;

bool find(ll x) {
    int i=2;
    while (i * i <= x) {
        if (i * i == x) {
            return true;
        }
    }
    return false;
}
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    int t;
    bool is_prime[]
    for (int i=1)
    cin >> t;
    while (t--) {
        ll x;
        cin >> x;
        // It is T-prime if sqrt of this number is a prime


    }

    return 0;
}

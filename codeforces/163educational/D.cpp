#include <stdio.h>
#include <string.h>

#include <algorithm>
#include <bitset>
#include <cmath>
#include <iomanip>
#include <iostream>
#include <iterator>
#include <limits>
#include <map>
#include <memory>
#include <mutex>
#include <new>
#include <numeric>
#include <ostream>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <vector>
// #include <unordered_map> // NEVER USE THOSE IN CP
// #include <unordered_set> // NEVER USE THOSE IN CP

#define int long long  // Because i'm so done with integer overflow mistakes

using namespace std;

signed main() {
    /*
    codecodecodecode
    code???codecode

    cccccccccccc
    ????????

    zaabaabz

    1. How to determine the longest tandem?

    Just brute force check. Assume the length of the tandem is k.
    Then you iterate over the string.
    You can binary search the tandem right? nope you can't
    What if it's like search for the longest tandem starting at this character?
    The possible candidates are limited. It's the set of next chracters.

    */
    ios::sync_with_stdio(0);
    cin.tie(0);

    int t;
    cin >> t;
    while (t--) {
        string s;
        cin >> s;
    }
}

return 0;
}

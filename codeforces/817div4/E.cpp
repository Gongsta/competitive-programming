#include <bits/stdc++.h>

typedef long long ll;

using namespace std;


int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    int t;
    cin >> t;
    while (t--) {
        int n,q;
        cin >> n >> q;
        int h[n];
        int w[n];
        for (int i=0;i<n;i++) {
            cin >> h[i];
            cin >> w[i];
        }
        while (q--) {
            int hs,ws,hb,wb;
            cin >> hs >> ws >> hb >> wb;
        }

    }

    return 0;
}

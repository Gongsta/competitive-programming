#include <bits/stdc++.h>

typedef long long ll;

using namespace std;


int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    int n, m;
    cin >> n >> m;
    ll a[m];
    for (int i=0;i<m;i++) {
        cin >> a[i];
    }
    int q;
    cin >> q;
    while (q--) {
        ll x_s,y_s,x_f,y_f,k;
        cin >> x_s >> y_s >> x_f >> y_f >> k;
        // I was thinking to do BFS but that is too slow?
    }

    return 0;
}

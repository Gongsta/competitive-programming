#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int n,m;
    cin >> n >> m;
    int u,v,l;
    while (m--) {
        cin >> u >> v >> l;

    }

    return 0;
}

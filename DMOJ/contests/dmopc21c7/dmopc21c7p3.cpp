#include <bits/stdc++.h>

using namespace std;

/*

I have the idea for how to solve this problem, but I am not sure
about how to solve it efficiently. I can break the problem down
to the fact that there are arrows, and you probably want to take care 
of the root ones first.

Then count the single nodes as well
*/
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    return 0;
}

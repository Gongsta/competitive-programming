#include <bits/stdc++.h>

using namespace std;

bool removed[101];
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int K, m;
    cin >> K >> m;
    int a;
    while (m--) {
        cin >> a;
        for (int i=1; i<=K;i++) {
            if 
        }
    }

    return 0;
}

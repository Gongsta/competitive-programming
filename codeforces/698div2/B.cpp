#include <bits/stdc++.h>

typedef long long ll;

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    int t;
    cin >> t;
    while (t--) {
        int q,d;
        cin >> q >> d;
        int a[q];
        for (int i=0;i<q;i++) {
            cin >> a[i];
            if (a[i] % 7 == 4 || (a[i] < 14)
        }


    }

    return 0;
}

#include <bits/stdc++.h>

using namespace std;

/*



*/

int arr[1000001];
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int N;
    cin >> N;
    for (int i=1;i<=N;i++) {
        cin >> arr[i];
    }
    for (int i=1;i<=N;i++) {
        for (int j=)
        cin >> arr[i];
    }

    return 0;
}

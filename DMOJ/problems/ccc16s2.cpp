#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int q,N;
    cin >> q >> N;
    int a, b;
    for (int i=1,i<=N, i++) {
        cin >> a;
    }

    for (int i=1,i<=N, i++) {
        cin >> a;
    }


    return 0;
}

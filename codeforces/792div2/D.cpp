#include <bits/stdc++.h>

using namespace std;

/*
Observations:
If k >= n, then the damage is 0
If k = 0, then the damage is just the sum
Else

*/
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    int t;
    cin >> t;
    while (t--) {
        int n,k;
        cin >> n >> k;
        int a[n+1];
        for (int i=1;i<=n;i++) {
            cin >> a[i];
        }
        int dp[n+1];
        for (int i=1;i<=n;i++) {
            cin >> a[i];
        }

    }

    return 0;
}
